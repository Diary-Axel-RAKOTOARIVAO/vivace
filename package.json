{
  "name": "vivace-monorepo",
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@changesets/cli": "^2.27.10",
    "shiki": "^4.3.1"
  },
  "private": true,
  "scripts": {
    "build": "bun run --filter 'vivace-css' build",
    "test": "bun run --filter 'vivace-css' test",
    "lint": "biome check .",
    "format": "biome format --write .",
    "docs:dev": "bun run --filter 'docs' dev",
    "docs:build": "bun run --filter 'docs' build",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "bun run build && changeset publish"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ]
}
